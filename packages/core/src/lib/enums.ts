export enum BranchType {
  Release = 'release',
  Prerelease = 'prerelease',
  Maintenance = 'maintenance',
}

export enum CommitType {
  Major = 'major',
  PreMajor = 'premajor',
  Minor = 'minor',
  PreMinor = 'preminor',
  Patch = 'patch',
  PrePatch = 'prepatch',
  PreRelease = 'prerelease',
}
