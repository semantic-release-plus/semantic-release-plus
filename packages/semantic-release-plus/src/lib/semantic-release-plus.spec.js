import { semanticReleasePlus } from './semantic-release-plus';
describe('semanticReleasePlus', () => {
  it('should work', () => {
    expect(semanticReleasePlus()).toEqual('semantic-release-plus');
  });
});
