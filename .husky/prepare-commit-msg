# #!/bin/sh
# . "$(dirname "$0")/_/husky.sh"

# COMMIT_MSG_FILE=$1
# COMMIT_SOURCE=$2
# SHA1=$3

# ts-node packages/commitmatic/src/bin/index.ts

# # Path to the custom commit message created by your Node.js script
# COMMITMATIC_MSG_FILE="$(git rev-parse --git-dir)/COMMITMATIC_EDITMSG"

# if [ -f "$COMMITMATIC_MSG_FILE" ]; then
#     # Concatenate the messages and overwrite the default commit message
#     {
#         cat "$COMMITMATIC_MSG_FILE"
#         echo ""
#         cat "$COMMIT_MSG_FILE"
#     } > "$COMMIT_MSG_FILE".new

#     # Replace the original message file with the concatenated one
#     mv "$COMMIT_MSG_FILE".new "$COMMIT_MSG_FILE"

#     # Optionally, remove the COMMITMATIC_EDITMSG file if no longer needed
#     rm "$COMMITMATIC_MSG_FILE"
# fi
# # Send the updated message from commitmatic to the message file and forward on
# # to the next hook


